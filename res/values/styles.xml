<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="ApplicationStyle" parent="android:Theme.NoTitleBar">
        <item name="android:buttonStyle">@style/button</item>
        <item name="android:windowBackground">@drawable/music_notes4</item>
    </style>

    <style name="button" parent="@android:style/Widget.Button">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:textColor">#FFFFFFFF</item>
        <item name="android:shadowColor">#FF000000</item>
        <item name="android:shadowDx">0</item>
        <item name="android:shadowDy">-1</item>
        <item name="android:shadowRadius">0.2</item>
        <item name="android:textSize">16dip</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="speaker_mode_button" parent="@style/button">
        <item name="android:background">@drawable/button_speaker</item>
    </style>

    <style name="broadcaster_mode_button" parent="@style/button">
        <item name="android:background">@drawable/button</item>
    </style>

    <style name="text_view" parent="@android:style/TextAppearance.Large">
        <item name="android:background">#d7e2ea</item>
        <item name="android:textColor">#151617</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="progress_bar" parent="@android:attr/progressBarStyleLarge">
        <item name="android:autoStart">true</item>
        <item name="android:background">@drawable/progress</item>
    </style>

    <style name="slave_play_text_view" parent="@android:style/TextAppearance.Medium">
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textColor">#923ac5</item>
        <item name="android:shadowColor">#fff</item>
        <item name="android:shadowDx">1</item>
        <item name="android:shadowDy">1</item>
        <item name="android:shadowRadius">50</item>
        <item name="android:textSize">35sp</item>
        <item name="android:gravity">center</item>
    </style>

</resources>